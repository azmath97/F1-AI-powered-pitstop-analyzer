# ML Package

Scaffold for future F1 strategy ML modules.

Planned package areas:

- `features`: feature extraction and validation.
- `training`: XGBoost, LightGBM, CatBoost model training.
- `inference`: model loading and prediction adapters.
- `explainability`: SHAP artifacts for API and dashboard use.

No models or business logic are implemented in this foundation.

