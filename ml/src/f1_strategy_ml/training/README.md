# Training

Future training pipelines should:

- Read curated datasets from `datasets/processed`.
- Track experiments in MLflow.
- Persist feature metadata and model signatures.
- Register candidate models before backend inference integration.

