# Datasets

Directory convention:

- `raw`: immutable extracts from FastF1, OpenF1, weather providers, and circuit metadata.
- `processed`: cleaned, joined, model-ready feature tables.
- `external`: manually supplied or third-party reference files.

Large data files are ignored by Git by default.

